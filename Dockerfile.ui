FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
# Streamlit + whatever your UI imports (requests if it calls the API)
RUN pip install --no-cache-dir streamlit requests -r requirements.txt
COPY . .
ENV STREAMLIT_SERVER_HEADLESS=true \
    STREAMLIT_SERVER_PORT=8501 \
    STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
EXPOSE 8501
CMD ["streamlit","run","ui_streamlit.py","--server.address=0.0.0.0","--server.port=8501"]
